import React from "react";
import "./Player";

const PlayerNameTitle = function (props) {
  return (
    <h2 className="name" id="name--0">
      {props.name}
    </h2>
  );
};

export default PlayerNameTitle;
